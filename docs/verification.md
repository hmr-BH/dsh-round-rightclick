# 圆盘菜单验证记录

## 已实现

- 默认直径 240 CSS 像素，外圈、扇区和中心按钮明确指定标准圆弧，局部覆盖宿主的超椭圆规则。
- 六个 SVG 图标，打断文案，悬停高亮，按需显示动作说明与禁用原因。
- 页面顶部居中的动作反馈：成功为绿色圆形对勾，失败为红色圆形感叹号，处理中为旋转图标；提示自动消失，完成后可手动关闭。保留重复点击保护、方向键导航和取消后焦点恢复。
- 通过对话节点身份或事件窗口解析分叉轮次，锁定打开菜单时的结束序号；不向未定位或未结束的轮次发送分叉请求。
- 分叉调用携带 `atSeq`，创建成功后打开子会话；缺少导航能力时提前失败，不显示虚假成功。

## 自动检查

- `pnpm install --frozen-lockfile --offline`：使用现有缓存完成，锁文件无需变更。`allowBuilds.esbuild` 已从提示字符串修正为布尔值。
- `pnpm run build`、`pnpm test`：在使用现有用户缓存的执行环境中通过，9 个文件、47 个用例通过。
- `node node_modules/typescript/bin/tsc --noEmit`：通过。
- `node build.mjs`：通过。
- `node node_modules/vitest/vitest.mjs run`：9 个文件、47 个用例通过。
- `scripts/verify-installed-dsh.mjs`：使用本机 `@deepseek-ai/dsh-api-session-controller` 0.1.2-rc.1 的真实切分实现验证构建产物。第一轮提问和回复保留 4 条事件，第二轮保留 8 条事件；未完成、空白、未加载位置不创建会话。源事件未改变，子会话身份传给打开与工作区挂接操作。

## 验证范围与待确认项

宿主契约检查将创建会话和工作区写入替换成内存记录，不执行模型请求、不写入用户会话。组件测试使用 jsdom 验证事件、焦点和反馈，不能证明实际浏览器绘制效果。

浏览器控制工具目前返回 `Codex auth token is unavailable`，以下实机验收尚未完成：

1. 在当前主题下确认缩小后的外圈与中心均为正圆，文字和图标清晰，窗口边缘没有截断。
2. 在真实会话第一轮提问和回复上分别分叉，确认进入新会话且不包含第二轮对话。
3. 确认实际浏览器的复制权限、成功提示和未完成轮次提示。

在上述实机验收有明确结果前，不将整体目标标记为全部完成。
